class Stack():
    def __init__(self):
        self.list = []
    
    def push(self,node):
    # 주어진 node값을 list에 추가
    # node가 이미 list에 존재할 시 ValueError발생
    # node를 push하는데 성공했을 시 True return
    
    def pop(self):
    # 가장 최근에 들어온 값이 list 에서 제거됨
    # pop 된 값 return
    
    def is_empty(self):
    #